// Every page, the header and the review workbench share these, so moving
// between them never changes how wide the application is.
export const PAGE_WIDTH = "mx-auto w-full max-w-[112rem] px-5 sm:px-8";
// Tailwind's h-14; the workbench starts right below the header.
export const HEADER_HEIGHT = "h-14";
export const BELOW_HEADER = "top-14";
